name: add_to_poem

on:
  schedule:
    - cron: "0 * * * *"    #run every hour on the hour, every day

jobs:
  add_to_poem:
    name : Add to Poem
    runs-on: ubuntu-latest

    steps:
      - name: checkout repo
        uses: actions/checkout@v2 # checkout the repository content to github runner.
      - name: run py script
        run: python ${{ env.DEVOPS_DIR }}/add_to_poem.py
        shell: sh
